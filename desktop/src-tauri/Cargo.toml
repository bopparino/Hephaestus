[package]
name = "hephaestus-desktop"
version = "0.9.0"
description = "Hephaestus — a local-first AI workspace with a real memory"
authors = ["Austin Cantrell"]
license = "MIT"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
dirs = "5"

[profile.release]
strip = true
lto = true
