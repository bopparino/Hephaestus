{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The Hephaestus window uses no IPC — it is a webview onto the local daemon.",
  "windows": ["main"],
  "permissions": []
}
